<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    package="com.compunetlimited.ogan.ebsu">

    <uses-permission android:name="android.permission.INTERNET" />

    <application
        android:allowBackup="true"
        android:icon="@drawable/ebsulogo"
        android:label="@string/app_name"
        android:roundIcon="@mipmap/ic_launcher_round"
        android:supportsRtl="true"
        android:theme="@style/AppTheme">
        <activity
            android:name=".LoginActivity">
            <mIntent-filter>
                <action android:name="android.mIntent.action.MAIN" />

                <category android:name="android.mIntent.category.LAUNCHER" />
            </mIntent-filter>
        </activity>
        <activity
            android:name=".RegisterActivity"
            android:label="@string/register"
            android:parentActivityName=".CheckIdActivity" />
        <activity
            android:name=".CheckIdActivity"
            android:label="@string/register"
            android:parentActivityName=".LoginActivity" />
        <activity
            android:name=".MainActivity"
            android:label="@string/title_activity_main"
            android:theme="@style/AppTheme.NoActionBar"/>
    </application>

</manifest>